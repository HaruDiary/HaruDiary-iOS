# HaruDiary: AI 작업 공통 지침

## 먼저 읽기

1. 이 문서
2. `docs/CONVENTIONS.md`: 코드·협업 규칙
3. `docs/REFACTORING_ROADMAP.md`: 순서·완료 조건
4. `docs/TESTING.md`: 검증 명령과 현재 CI의 범위

사용자의 현재 요청이 작업 범위를 결정한다. 로드맵의 다음 항목을 임의로 시작하지 않는다.
이 문서는 Codex 등 AGENTS.md 지원 도구의 공통 진입점이다. 다른 AI 도구는 이 파일을
명시적으로 읽도록 설정하고 규칙 본문을 별도 파일에 복제하지 않는다.

## 제품과 보존해야 할 것

- 기존 Firebase 계정·일기·사진·문서 ID·저장 필드의 호환성을 유지한다.
- 순서는 로직 정리 → 기존 UIKit에서 검증 → SwiftUI 화면 교체다.
- 담당자 배정은 미정이다. AI가 특정 사람에게 작업을 배정하지 않는다.
- 마을 상세 기획과 구현은 추후 논의한다. 현재 리팩토링에 보상 규칙·새 마을 스키마를 추가하지 않는다.
- 승인되지 않은 데이터 마이그레이션, 운영 데이터 변경, 대규모 의존성 갱신을 하지 않는다.

## 작업 절차

1. `git status --short --branch`와 관련 소스·호출부·테스트를 읽는다.
2. 변경할 책임과 파일, 보존할 동작, 검증 방법을 짧게 설명한다.
3. 재구조화 전 해당 동작을 테스트로 고정한다. 버그 수정은 실패 재현부터 한다.
4. 요청한 범위의 최소 변경을 수행한다. 다른 사람이 만든 변경을 되돌리지 않는다.
5. 영향받는 테스트를 실행한다. 앱 소스·프로젝트·패키지 변경 시 앱 빌드도 실행한다.
6. 변경점, 실행한 명령, 실제 결과, 검증하지 못한 부분을 보고한다.

## 구조

- UI는 표시·입력·탐색을 담당한다. Firebase 호출과 비즈니스 로직을 새 뷰에 넣지 않는다.
- 도메인 로직은 UIKit/SwiftUI/Firebase에 의존하지 않도록 추출한다.
- 저장 DTO와 도메인 모델의 변환에서 기존 스키마를 보존한다.
- Repository의 프로토콜과 실제 구현을 분리하고 필요한 의존성만 주입한다.
- 새로운 전역 singleton, 무분별한 NotificationCenter 이벤트, 억지 범용 계층을 늘리지 않는다.
- 상태를 UI에 반영하는 객체의 main actor 경계와 비동기 작업 취소·listener 해제를 명시한다.
- 테스트 편의를 위해 실제 앱과 다른 비즈니스 로직 사본을 만들지 않는다.

## 검증

```sh
bash scripts/ci.sh test
bash scripts/ci.sh build
```

- 단위 테스트는 앱을 실행하지 않고 실제 `DataModel.swift`를 컴파일한다.
- CI용 Firebase plist는 빌드용 가짜 값이다. 인증·동기화 성공의 증거가 아니다.
- 앱 실행·기능 검증이 필요한 작업은 실제 시뮬레이터/기기에서 해당 흐름을 확인한다.
- 테스트 삭제·skip·실패 무시로 CI를 통과시키지 않는다.
- 실행하지 않은 테스트나 화면 확인을 성공했다고 쓰지 않는다.

## 보안과 변경 범위

- `GoogleService-Info.plist`, 인증서, 토큰, 실제 날씨 API 키를 커밋하거나 로그에 노출하지 않는다.
- 개발자의 실제 Firebase 파일을 CI용 값으로 덮어쓰지 않는다.
- 테스트는 운영 Firebase에 쓰지 않는다. 통합 테스트는 별도 Emulator를 사용한다.
- 강제 push, 공유 브랜치 이력 재작성, 운영 데이터 삭제는 일반 리팩토링에 포함되지 않는다.
- 기존 코드 전체 포맷 변경과 폴더 전체 이동을 기능 변경 PR에 섞지 않는다.
- 새로운 테스트 파일/소스를 추가하면 Xcode 타깃 연결과 공유 스킴도 확인한다.
